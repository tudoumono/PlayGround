# ==============================================================================
# AutoSlideGen 環境変数設定例
# ==============================================================================
# このファイルを .env にコピーして、実際の値を設定してください。
# cp .env_example .env
# ==============================================================================

# ==============================================================================
# 1. AWS認証情報（Lambda関数へのアクセス用）
# ==============================================================================
AWS_ACCESS_KEY_ID="YOUR_AWS_ACCESS_KEY_ID"
AWS_SECRET_ACCESS_KEY="YOUR_AWS_SECRET_ACCESS_KEY"
# 一時的な認証情報（IAMロールなど）を使用する場合は、以下の行も追加
# AWS_SESSION_TOKEN="YOUR_SESSION_TOKEN"

# ==============================================================================
# 2. API Gateway設定（Lambda関数のエンドポイント）
# ==============================================================================
API_GATEWAY_HOSTNAME="xxxxxx.execute-api.ap-northeast-1.amazonaws.com"
API_GATEWAY_REGION="ap-northeast-1"
API_GATEWAY_URL="https://xxxxxx.execute-api.ap-northeast-1.amazonaws.com/your_stage/your_resource"

# PowerPoint生成用エンドポイント（lambda-pptx-generator.py用）
API_GENERATE_ENDPOINT_URL="https://xxxxxxxxxx.execute-api.ap-northeast-1.amazonaws.com/your-stage/generate"

# URL取得用エンドポイント（lambda-pptx-get_download_url.py用）
API_GET_URL_ENDPOINT_URL="https://yyyyyyyyyy.execute-api.ap-northeast-1.amazonaws.com/your-stage/get-url"

# ==============================================================================
# 3. Lambda関数の環境変数設定
# ==============================================================================
# これらの値はLambda関数のコンソールで設定する必要があります。
# ローカル開発時にも同じ値を使用する場合は、ここに記載してください。

# -----------------------------------------------------------------------------
# 3.1 必須設定
# -----------------------------------------------------------------------------
# S3バケット名（PowerPointファイルの保存先）
S3_BUCKET_NAME="your-presentation-bucket"

# -----------------------------------------------------------------------------
# 3.2 セキュリティ設定
# -----------------------------------------------------------------------------
# CORS許可オリジン（本番環境では特定ドメインに限定推奨）
ALLOWED_ORIGINS="*"  # 例: "https://myapp.example.com"

# 署名付きURLの有効期限（秒）
PRESIGNED_URL_EXPIRY="3600"  # 1時間

# -----------------------------------------------------------------------------
# 3.3 S3設定
# -----------------------------------------------------------------------------
# S3内のファイル保存パス・プレフィックス
S3_PREFIX="presentations/"  # 例: "presentations/dev/", "presentations/prod/"

# AWSリージョン
AWS_REGION="ap-northeast-1"

# -----------------------------------------------------------------------------
# 3.4 ブランディング設定
# -----------------------------------------------------------------------------
# ヘッダーロゴURL
LOGO_HEADER_URL="https://upload.wikimedia.org/wikipedia/commons/thumb/2/2f/Google_2015_logo.svg/1024px-Google_2015_logo.svg.png"

# クロージングスライドのロゴURL
LOGO_CLOSING_URL="https://upload.wikimedia.org/wikipedia/commons/thumb/2/2f/Google_2015_logo.svg/1024px-Google_2015_logo.svg.png"

# フッターに表示する組織名
FOOTER_ORGANIZATION_NAME="Your Organization"

# -----------------------------------------------------------------------------
# 3.5 デザイン設定 - フォント
# -----------------------------------------------------------------------------
# デフォルトフォントファミリー
DEFAULT_FONT_FAMILY="Arial"  # 例: "Noto Sans JP", "Yu Gothic"

# フォントサイズ設定
FONT_SIZE_TITLE="45"
FONT_SIZE_SECTION="38"
FONT_SIZE_CONTENT_TITLE="28"
FONT_SIZE_SUBHEAD="18"
FONT_SIZE_BODY="14"
FONT_SIZE_FOOTER="9"

# -----------------------------------------------------------------------------
# 3.6 デザイン設定 - カラーパレット
# -----------------------------------------------------------------------------
# プライマリーカラー（メインテーマ色）
THEME_COLOR_PRIMARY="4285F4"  # Google Blue

# セカンダリーカラー
THEME_COLOR_RED="EA4335"      # Google Red
THEME_COLOR_YELLOW="FBBC04"   # Google Yellow
THEME_COLOR_GREEN="34A853"    # Google Green

# テキストカラー
TEXT_PRIMARY_COLOR="333333"   # ダークグレー
TEXT_WHITE_COLOR="FFFFFF"     # 白

# 背景カラー
BACKGROUND_WHITE_COLOR="FFFFFF"  # 白背景
BACKGROUND_GRAY_COLOR="F8F9FA"   # ライトグレー背景

# カード要素のカラー
CARD_BG_COLOR="FFFFFF"        # カード背景色
CARD_BORDER_COLOR="DADCE0"    # カード枠線色

# -----------------------------------------------------------------------------
# 3.7 スライドサイズ設定
# -----------------------------------------------------------------------------
# スライドの幅（ピクセル）
SLIDE_WIDTH_PX="960"

# スライドの高さ（ピクセル）
SLIDE_HEIGHT_PX="540"

# -----------------------------------------------------------------------------
# 3.8 デバッグ設定
# -----------------------------------------------------------------------------
# デバッグモード（開発環境では true、本番環境では false）
DEBUG_MODE="false"

# 詳細エラー表示（開発環境では true、本番環境では false）
VERBOSE_ERRORS="false"


# ==============================================================================
# 4. その他のオプション設定
# ==============================================================================
# ログレベル（DEBUG, INFO, WARNING, ERROR, CRITICAL）
LOG_LEVEL="INFO"

# タイムゾーン
TIMEZONE="Asia/Tokyo"

# ==============================================================================
# 使用上の注意
# ==============================================================================
# 1. このファイルには機密情報が含まれるため、.gitignoreに追加されています
# 2. 本番環境では、AWS Systems Manager Parameter StoreやSecrets Managerの使用を推奨
# 3. 環境変数の値は、ダブルクォート（"）で囲むことを推奨
# 4. カラーコードは、#を除いた6桁の16進数で指定（例: "4285F4"）
# ==============================================================================
